import fastify from "fastify";

const router = fastify.Router();

//User
router.post("/create", );

//Stock
router.post("/create", );

//Shelf
router.post("/create", );

//Product
router.post("/create", );

export default router;